#pragma once
#include "../sys_graph.hpp"
namespace graph {

    class event_sub { // Event class that actually consumes os events

    };
    template <typename event_sub, >
    class input_event : graph::event {
        using graph::event::event ;
        input_event() ;

    };
    class
    class entity_event

};
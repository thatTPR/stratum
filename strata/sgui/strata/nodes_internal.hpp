#include "nodes_editor.hpp"

class canvas_tree  {
    pin* roots[];
    pin* leafs[];
};
// Generates one node from canvas
class canvas_builder {
    canvas_tree ctree ; 

    void build_tree(node_canvas* can){
        
    };
    canvas_builder()
};


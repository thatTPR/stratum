#include <SDL2/SDL.h>
class hi_impl {

    virtual enum event ;
    typedef class enum {

    } ;
    virtual void init() ;
    virtual 
    virtual union void PollEvent(union event*);
    virtual int GetWindows();
    virtual bool CreateWindows();
    virtual unsigned uint GetMouseButton(unsigned int mouse);


};
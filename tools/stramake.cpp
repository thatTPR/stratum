#include <filesystem>
#define fs std::filesystem
#include <string>
#include <vector>
#include <iostream>
#include <system>
#include <fstream>
int main(int argc,char* argv[argc]){
    if(argc==1){std::cout<<"stramake command"}
    for(int i = 0 ; i <argc;i++){
        if(argv[i][0])
    };
};
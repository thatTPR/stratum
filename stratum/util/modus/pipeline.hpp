#include <filesystem>
#include "../sys.hpp"

class shader {

};

class pipelineBuilder {
    build(pipeline::  )
};

class compiler {
    class pipelineBuilder {

    };

    void toSource(std::path path)
    
};
#include <system>
#include "util/sys.cpp"
#include "../lib/sei_settings.hpp"

setDefaults()
                
 parseSettings()
                
 f(this->file);
if (!f.is_open()
 jsonString(this->setDefaults()
 jsonString((std::istreambuf_iterator<char>(f)
close();
parse(jsonString);
catch (const std::exception &e)
                    
Logging("ProjectSettings.json Parsing error: " + e.what()
projectSettings() 
 resourcesChanged()
cxxCompile()
 run()
                
 save()
                
Sproject (json file format)
                
 saveSettings()
 hotReload()
                
 hotReloadEnded()
                
 projectParse(std::string name)
 searchProject()
                
project(std::string ProjectPath, std::string name)
                
StrataLog();
open(projectPath++ name);
if (!in.is_open()
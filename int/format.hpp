#include <string>
#include <cwctype>
#include <fstream>
template <typename _Char  >




class parser {
    public:    
    virtual void write();
    parser(ifstream f){

    };
    parser(std::path p){

    };
    parser(std::string& s)
};


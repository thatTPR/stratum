// Serilization and deserialization library , probably stupid 
#pragma once
#include <vector>
#include <>
template <struct s , typename chartype>

class stream_proc {

};
 class serial {
    s ; 
    struct s 
    char go_through(){

    };
    virtual string operator()(s){
        reinterpret_cast<>
    } ; 
    ser();
};
template <struct s, typename chartype>
s deser (string){

};
